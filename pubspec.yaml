name: mindquest
description: "Gamified AI Chatbot Quiz App for Mental Health Awareness"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12

  # State Management
  provider: ^6.1.5

  # Animations
  flutter_animate: ^4.5.0

  # Utilities
  intl: ^0.19.0
  google_sign_in: ^6.2.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/questions.json
